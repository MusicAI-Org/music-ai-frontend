import { borderRadius } from "../../base/borderRadius";

const Skeleton = {
  baseStyle: {},

  sizes: {},

  variants: {
    default: {
      borderRadius: borderRadius.sm,
    },
  },

  defaultProps: {
    variant: "default",
  },
};

export default Skeleton;
